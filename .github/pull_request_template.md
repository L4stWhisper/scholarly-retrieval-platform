## Problem and scope

## Implementation

## Verification

- [ ] `uv run ruff check src tests tools`
- [ ] `uv run python -m compileall -q src tests`
- [ ] `uv run pytest -q`
- [ ] User-facing documentation updated (README entry point, the matching `docs/` guide,
      `docs/technical-implementation.md`, and `docs/test-acceptance.md` where behaviour changed)
- [ ] Commit messages follow Conventional Commits (`feat`, `fix`, `docs`, `refactor`, `test`, `chore`, `ci`)
- [ ] No credentials, restricted full text, caches, or local databases committed

## Provider, licensing, or deployment impact
